{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-parser",
            "type": "shell",
            "command": "go build -o ./bin/GoParser ${workspaceFolder}",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}